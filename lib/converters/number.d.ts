import { Options } from './../types/index.d';
import { Parameter, FormKitInput } from "../types";
export declare function isInteger(param: Parameter): boolean;
export declare function convertNumber(param: Parameter, options: Options): FormKitInput;
